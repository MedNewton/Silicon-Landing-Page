import { Stack, Typography } from "@mui/material";
const Footer = () => {
  return (
    <Stack component="footer" sx={{ py: 4 }}>
      <Typography variant="h6">Footer</Typography>
    </Stack>
  );
};

export default Footer;